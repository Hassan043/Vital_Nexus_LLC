# VitalNexus.Contracts

This project contains shared contract types used across multiple application layers, such as API request/response models and shared DTOs.

## Usage

Reference this project from consumers like the API, Functions, or other shared libraries.
